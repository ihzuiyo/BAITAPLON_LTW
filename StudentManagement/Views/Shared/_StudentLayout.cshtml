<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Classes369 Sinh Viên</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="~/css/student.css" asp-append-version="true" />
    <link href="#" rel="stylesheet" />
</head>
<body>
    <div class="wrapper">
        <!-- Sidebar -->
        <nav id="sidebar" class="sidebar">
            <div class="sidebar-header">
                <div class="logo-container">
                    <i class="fas fa-graduation-cap logo-icon"></i>
                    <h3>CLASSES369</h3>
                </div>
                <p class="subtitle">Sinh Viên</p>
            </div>

            <ul class="list-unstyled components">
                <li class="@(ViewContext.RouteData.Values["Action"]?.ToString() == "Dashboard" ? "active" : "")">
                    <a asp-controller="Student" asp-action="Dashboard">
                        <i class="fas fa-home"></i> Trang Chủ
                    </a>
                </li>
                <li class="@(ViewContext.RouteData.Values["Action"]?.ToString() == "MyClasses" ? "active" : "")">
                    <a asp-controller="Student" asp-action="MyClasses">
                        <i class="fas fa-book-open"></i> Lớp Học Của Tôi
                    </a>
                </li>
                <li class="@(ViewContext.RouteData.Values["Action"]?.ToString() == "MyScores" ? "active" : "")">
                    <a asp-controller="Student" asp-action="MyScores">
                        <i class="fas fa-chart-bar"></i> Điểm Số
                    </a>
                </li>
                <li class="@(ViewContext.RouteData.Values["Action"]?.ToString() == "Schedule" ? "active" : "")">
                    <a asp-controller="Student" asp-action="Schedule">
                        <i class="fas fa-calendar-alt"></i> Thời Khóa Biểu
                    </a>
                </li>
                <li class="@(ViewContext.RouteData.Values["Action"]?.ToString() == "Tuition" ? "active" : "")">
                    <a asp-controller="Student" asp-action="Tuition">
                        <i class="fas fa-money-bill-wave"></i> Học Phí
                    </a>
                </li>
                <li class="@(ViewContext.RouteData.Values["Action"]?.ToString() == "Notifications" ? "active" : "")">
                    <a asp-controller="Student" asp-action="Notifications">
                        <i class="fas fa-bell"></i> Thông Báo
                    </a>
                </li>
            </ul>

            <div class="sidebar-footer">
                <a href="#" class="logout-btn">
                    <i class="fas fa-sign-out-alt"></i> Đăng Xuất
                </a>
            </div>
        </nav>

        <!-- Page Content -->
        <div id="content">
            <nav class="navbar navbar-expand-lg navbar-light bg-light top-navbar">
                <div class="container-fluid">
                    <button type="button" id="sidebarCollapse" class="btn btn-info">
                        <i class="fas fa-bars"></i>
                    </button>
                    <div class="ml-auto d-flex align-items-center">
                        <div class="user-info">
                            <i class="fas fa-user-circle"></i>
                            <span>Sinh Viên</span>
                        </div>
                    </div>
                </div>
            </nav>

            <main role="main" class="main-content">
                @RenderBody()
            </main>

            <footer class="footer">
                <div class="container-fluid">
                    <span>&copy; 2025 - Classe369 - Hệ Thống Quản Lý Trung Tâm Tin Học</span>
                </div>
            </footer>
        </div>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/student.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>